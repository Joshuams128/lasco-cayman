// TODO: Fetch store locations from Sanity
// import { client } from "@/sanity/client";

export default function StoreListPage() {
  // TODO: Fetch stores
  // const stores = await client.fetch(`*[_type == "storeLocation"]`);

  return (
    <div className="mx-auto max-w-7xl px-4 py-8">
      <h1 className="mb-8 text-3xl font-bold">Store Locations</h1>

      <p className="text-gray-500">
        TODO: Fetch and display store locations from Sanity
      </p>

      {/* TODO: Store location cards with address, phone, hours */}
    </div>
  );
}
